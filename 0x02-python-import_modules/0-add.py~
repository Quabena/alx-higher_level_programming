#!/usr/bin/python3

if __name__ == "__main__":
    # Importing the add function from add_0
    from add_0 import add

    # Assign values to variables a and b
    a = 1
    b = 2

    # Call the add function with a and b and print the result
    print("{} + {} = {}".format(a, b, add(a, b)))

