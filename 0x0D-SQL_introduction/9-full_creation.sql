-- A script that creates second_table and insert multiple rows
-- Syntax: CREATE TABLE IF NOT EXISTS and INSERT INTO

CREATE TABLE IF NOT EXISTS second_table (
	id INT,
	name VARCHAR(256),
	score INT
);
